# python
.venv/
__pycache__/
*.pyc
# node
node_modules/
dist/
# data — never commit audio or transcripts
data/
*.wav
*.sqlite
*.db
eval/audio/*
!eval/audio/.gitkeep
# models
models/
.build/
# macOS finder cruft
.DS_Store
# built app bundles — assembled by app/build-app.sh, not source
*.app/
Transcriber*.zip
# Xcode per-user state regenerated on open
.swiftpm/
